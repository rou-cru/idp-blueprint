apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    version: 4.9.2 # Using a recent stable version
    releaseName: jenkins
    namespace: cicd
    valuesFile: jenkins-values.yaml
