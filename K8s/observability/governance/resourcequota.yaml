apiVersion: v1
kind: ResourceQuota
metadata:
  name: observability-quota
  namespace: observability
spec:
  hard:
    requests.cpu: "2000m"
    requests.memory: "6Gi"
    limits.cpu: "4000m"
    limits.memory: "12Gi"
