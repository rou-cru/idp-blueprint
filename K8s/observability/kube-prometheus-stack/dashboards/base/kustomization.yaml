apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability

configMapGenerator:
  - name: custom-dashboards-all
    # Kustomize will load all files from this directory
    files:
      - json
    options:
      labels:
        grafana_dashboard: "1"