apiVersion: v1
kind: ResourceQuota
metadata:
  name: security-quota
  namespace: security
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "2Gi"
    limits.cpu: "1500m"
    limits.memory: "4Gi"
