apiVersion: v1
kind: ResourceQuota
metadata:
  name: security-quota
  namespace: security
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  hard:
    requests.cpu: "700m"
    requests.memory: "1Gi"
    limits.cpu: "3000m"
    limits.memory: "3Gi"
