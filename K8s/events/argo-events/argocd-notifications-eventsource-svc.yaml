apiVersion: v1
kind: Service
metadata:
  name: argocd-notifications-eventsource-svc
  namespace: argo-events
  labels:
    app.kubernetes.io/part-of: idp
    app.kubernetes.io/component: eventing
    app.kubernetes.io/name: argocd-notifications-eventsource
spec:
  type: ClusterIP
  ports:
    - port: 12002
      targetPort: 12002
      protocol: TCP
      name: webhook
  selector:
    eventsource-name: argocd-notifications
