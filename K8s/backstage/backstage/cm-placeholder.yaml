apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-app-config-override
  namespace: backstage
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
data:
  app-config.override.yaml: |
    # PLACEHOLDER: This content will be replaced by the config-renderer job
    backend:
      baseUrl: https://backstage.placeholder.local
