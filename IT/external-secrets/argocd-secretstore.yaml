apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: argocd
  labels:
    app.kubernetes.io/name: secretstore
    app.kubernetes.io/instance: vault-backend
    app.kubernetes.io/component: secret-sync
    app.kubernetes.io/part-of: idp
    owner: platform-team
    business-unit: infrastructure
    environment: demo
  annotations:
    description: 'SecretStore connecting to Vault for ArgoCD secrets'
    contact: platform-team
spec:
  retrySettings:
    maxRetries: 5
    retryInterval: "10s"
  provider:
    vault:
      server: "https://vault.vault-system.svc.cluster.local:8200"
      path: "secret"
      version: "v2"
      caProvider:
        type: Secret
        name: idp-demo-ca-secret
        namespace: cert-manager
        key: ca.crt
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "eso-argocd-role"
          serviceAccountRef:
            name: external-secrets
            audiences:
              - vault
