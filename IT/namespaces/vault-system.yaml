# Defines the namespace for HashiCorp Vault.
apiVersion: v1
kind: Namespace
metadata:
  name: vault-system
  labels:
    app.kubernetes.io/part-of: idp
    owner: platform-team
    business-unit: infrastructure
    environment: demo
  annotations:
    description: "Namespace for HashiCorp Vault secrets backend."
