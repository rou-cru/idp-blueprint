# Root Catalog for IDP Blueprint
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: idp-blueprint-root
  description: Entry point for the IDP Blueprint catalog.
spec:
  type: file
  targets: []
    # Auto-discovery is enabled via GithubEntityProvider.
    # All catalog-info.yaml files in the repo will be automatically registered.
