[project]
name = "idp-blueprint-docs"
version = "0.1.0"
description = "Documentation tooling for the IDP Blueprint MkDocs site"
requires-python = ">=3.12"
dependencies = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
]

[tool.uv]
