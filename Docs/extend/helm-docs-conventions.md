# Helm-docs Conventions

Consistent documentation for Helm values using helm-docs.

## Comment Style

- `## @section` to group values
- `# -- description` for each value
- `# @default -- <value>` to capture defaults

## Validation

- Run `./Scripts/helm-docs-lint.sh` before PRs

