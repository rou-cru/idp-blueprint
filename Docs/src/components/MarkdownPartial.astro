---
import { marked } from 'marked';

interface Props {
  // Raw markdown string generated by helm-docs (tables, headings, etc.)
  content: string;
}

const { content } = Astro.props;
const html = marked.parse(content);

---

<div class="markdown-partial" set:html={html} />
