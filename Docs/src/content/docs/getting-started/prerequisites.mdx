---
title: Getting Started - Prerequisites
sidebar:
  label: Prerequisites
  order: 2
---
import { LinkButton } from '@astrojs/starlight/components';

Before deploying the IDP Blueprint, verify your environment with this quick checklist.

## System Requirements

<div class="grid cards" markdown>

- **Hardware**

    ---

  - CPU: 4 cores (6+ recommended)
  - Memory: 8 GB (12+ GB recommended)
  - Storage: 20+ GB free

- **Operating System**

    ---

  - Linux, macOS, or Windows (WSL2)

- **Network & Ports**

    ---

  - Internet to pull images
- NodePorts free: values from `config.toml` (`nodeport_http`/`nodeport_https`, defaults 30080/30443)

</div>

## Software Dependencies

<div class="grid cards" markdown>

- **Core Tools**

    ---

  - Docker (and `docker login`)
  - Git (2.0+)
  - VS Code + Dev Containers (recommended) or Devbox

- **CLI Tooling (if no Dev Container)**

    ---

  - k3d, kubectl, helm, kustomize
  - envsubst (gettext), dasel, task

</div>

:::note
Dev Containers already include all required tooling (kubectl, helm, k3d, task, etc.). Opening the repo in VS Code and choosing “Reopen in Container” is the easiest path.
:::

:::note[Cluster resource sizing (demo)]
For a full demo cluster on a single machine, we see good results with:

- **Minimum configuration**: 4 CPU cores, 8 GB RAM
- **Recommended configuration**: 6 CPU cores, 12 GB RAM
- **Storage**: ~20 GB persistent storage
:::

## Docker Hub Authentication

To avoid Docker Hub rate limiting:

1. Create an account if you don’t have one
2. Run `docker login` before `task deploy`

## Optional

- Modern browser with DevTools
- Your preferred editor/IDE

<div style="display: flex; gap: 0.75rem; justify-content: center; margin-top: var(--sl-spacing-4);">
  <LinkButton href="/getting-started/overview/" variant="secondary" icon="arrow-left">
    Back: Overview
  </LinkButton>
  <LinkButton href="/getting-started/quickstart/" variant="primary" icon="arrow-right">
    Next: Quickstart
  </LinkButton>
</div>
