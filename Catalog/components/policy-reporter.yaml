apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: policy-reporter
  description: Policy Reporter - Monitoring and reporting for Kyverno policy violations.
  tags: [policy, reporting, monitoring, dashboard]
  annotations:
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: policy-reporter
    backstage.io/kubernetes-namespace: kyverno-system
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=policy-reporter"
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: idp-security
  dependsOn:
    - component:kyverno
