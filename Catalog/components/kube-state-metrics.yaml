apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: kube-state-metrics
  description: Kube State Metrics - Exposes Kubernetes object metrics.
  tags: [metrics, kubernetes, prometheus, tier-high, environment-demo, platform-observability, data-pipeline-metrics, collector]
  annotations:
    backstage.io/source-location: url:https://github.com/rou-cru/idp-blueprint/tree/main/Catalog/components/kube-state-metrics.yaml
    backstage.io/kubernetes-id: kube-state-metrics
    backstage.io/kubernetes-namespace: observability
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=kube-state-metrics"
    argocd/app-name: obs-kube-prometheus-stack
    # Kyverno Policy Reporter
    kyverno.io/namespace: observability
    kyverno.io/kind: Deployment
    kyverno.io/resource-name: prometheus-kube-state-metrics
spec:
  type: service
  lifecycle: production
  owner: platform-team
  dependsOn:
    - resource:default/local-k8s-cluster
  system: idp-observability
  subComponentOf: kube-prometheus-stack
