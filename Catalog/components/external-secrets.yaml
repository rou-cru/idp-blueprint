apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: external-secrets
  description: External Secrets Operator - Synchronizes secrets from external sources - Helm Chart.
  tags: [secrets, operator, vault, security, tier-critical, environment-demo, platform-infrastructure]
  annotations:
    backstage.io/source-location: url:https://github.com/rou-cru/idp-blueprint/tree/main/Catalog/components/external-secrets.yaml
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: external-secrets
    backstage.io/kubernetes-namespace: external-secrets-system
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=external-secrets"
    deployment.method: "helm"
    helm.chart: "external-secrets/external-secrets-operator"
    # Kyverno Policy Reporter
    kyverno.io/namespace: external-secrets-system
    kyverno.io/kind: Deployment
    kyverno.io/resource-name: external-secrets
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: idp-security
  dependsOn:
    - resource:default/local-k8s-cluster
    - component:vault
