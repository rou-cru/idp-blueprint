apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: external-secrets
  description: External Secrets Operator - Synchronizes secrets from external sources - Helm Chart.
  tags: [secrets, operator, vault, security]
  annotations:
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: external-secrets
    backstage.io/kubernetes-namespace: external-secrets-system
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=external-secrets"
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: idp-security
  dependsOn:
    - component:vault
