apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: trivy-operator
  description: Kubernetes-native security scanner.
  tags: [security, scanning, vulnerability, tier-high, environment-demo, platform-security]
  annotations:
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=trivy-operator"
    deployment.method: "helm"
    helm.chart: "aqua/trivy-operator"
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: idp-security
  dependsOn:
    - resource:trivy-storage
