apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: alertmanager
  description: Alertmanager - Handles alerts sent by client applications such as the Prometheus server.
  tags: [alerts, notifications, prometheus, observability, tier-critical, environment-demo, platform-observability]
  annotations:
    backstage.io/source-location: url:https://github.com/rou-cru/idp-blueprint/tree/main/Catalog/components/alertmanager.yaml
    backstage.io/kubernetes-id: alertmanager
    backstage.io/kubernetes-namespace: observability
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=alertmanager"
    argocd/app-name: obs-kube-prometheus-stack
    deployment.method: "helm"
    helm.chart: "prometheus-community/kube-prometheus-stack"
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: idp-observability
  subComponentOf: kube-prometheus-stack
  dependsOn:
    - component:kube-prometheus-stack
