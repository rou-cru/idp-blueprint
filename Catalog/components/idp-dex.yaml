apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: idp-dex
  description: OIDC Identity Provider (Dex).
  tags: [auth, oidc, go]
  annotations:
    backstage.io/kubernetes-label-selector: "app.kubernetes.io/name=dex"
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: idp-core
